{
  "name": "usms-enterprise-mobile",
  "version": "8.1.0",
  "description": "USMS Enterprise Mobile v8.1 - Telegram-like clean redesign and working settings preview",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}